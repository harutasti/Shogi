# 対局／解析 情報分離 採用判断

- 対象Issue: <https://github.com/harutasti/Shogi/issues/9>
- 状態: Computer Use比較完了・初見ユーザーテスト待ち
- 判断: Aを第一候補（本命仮説）として現行版と初見比較する。本番採用は未決定
- 方針更新日: 2026-07-23

## ベースライン要約

2026-07-21に現行公開版をComputer Useで測定した。5シナリオはデスクトップ、400px幅ともすべて完了でき、機能的な阻害はなかった。

| 環境 | 完了 | 合計クリック | 合計スクロール | 自動操作経過秒 |
|---|---:|---:|---:|---:|
| Desktop Chrome | 5 / 5 | 9 | 3 | 9.358 |
| 400 × 625 | 5 / 5 | 9 | 7 | 21.936 |

主要な観察:

- デスクトップでは盤と操作パネルを横並びで確認できるが、解析結果の展開とKIF読込後の棋譜増加により、解析カードが下へ移動する。
- 400px幅では初期表示が盤中心のため、対局開始に1回スクロールが必要になる。
- 400px幅で「対局カード → 盤で着手 → 棋譜確認」は上下2回のスクロールが必要になる。
- 400px幅で「KIF読込 → 3手目 → 解析結果」は2回のスクロールが必要になる。
- 解析結果は棋譜カードより下、保存・共有は解析結果より上にあるため、作業目的を切り替えるたびに上下移動が発生する。

この値は正解経路を知るComputer Useの一貫ベンチマークであり、人間の発見性や理解を示さない。経過秒にはComputer Useの待機時間を含む。初見ユーザーテストは未実施である。

## プロトタイプ比較

| 案 | 強み | 弱み | 定量結果 | 定性結果 |
|---|---|---|---|---|
| A モード切替 | 対局／解析の目的が明確。盤・棋譜・選択局面を切替後も保持 | 400px幅の合計スクロール7回で現行版と同じ。解析到達の改善なし | Desktop: 11クリック・0スクロール・8.077秒。400px: 11クリック・7スクロール・24.962秒 | 区分は最も明快だが、狭い画面では操作カード間の上下移動が残る |
| B 展開式解析 | 盤と棋譜を残したまま解析。400pxの解析シナリオでスクロールを3回から1回へ66.7%削減 | 開閉クリックが増える。保存・共有へ戻る際にシートを閉じる必要 | Desktop: 12クリック・0スクロール・7.997秒。400px: 12クリック・5スクロール・21.533秒 | 解析の局所タスクに最も強い。非モーダルのため背景状態も保てる |
| D 情報階層改善 | 全体のクリック・スクロール・経過秒が3案中最少。目的に応じてCTAが更新 | 解析結果の挿入で位置が動き、400pxのUX-03では確認に1回スクロール | Desktop: 9クリック・0スクロール・6.337秒。400px: 9クリック・4スクロール・17.701秒 | 現行構造からの変化が小さく、主要経路が短い。文脈CTAの理解は人間確認が必要 |

全30ケースはPass、誤操作・復帰操作・状態消失は0件だった。経過秒はComputer Use待機を含み、人間の操作速度とは比較しない。

400px幅のUX-03・UX-04を「解析到達」として合算すると、現行版3スクロールに対しAは3、Bは1、Dは2。仮置きした50%削減目標を自動操作上で満たしたのはBだけである。一方、全5シナリオの合計はDが最少だった。

## アクセシビリティと状態管理

- Aの表示区分は`aria-pressed`を持つボタン群、A／B／Dの比較切替は`tablist`／`tab`で表現した。
- 比較タブは左右矢印、Home、Endで移動でき、フォーカスリングを表示する。
- 3案とも盤、棋譜、現在手数、解析結果を案の切替後も保持する。
- Bの解析シートは背景を操作不能にしない非モーダル構造とし、閉じる操作を明示した。
- Reduced Motion、Reduced Transparency、Increased ContrastのCSS代替を用意した。
- スクリーンリーダーによる読み上げ順と、初見利用者が文脈CTAを正しく理解できるかは未検証である。

## 実装・保守コスト

| 案 | 相対コスト | 主な実装・保守要素 |
|---|---|---|
| A | 中 | 目的別パネル、モードURL／復元、共通状態の保持、モバイル固定切替 |
| B | 高 | レスポンシブシート、フォーカスと閉じる挙動、背景との同時操作、重なり管理 |
| D | 低〜中 | 文脈CTAの状態機械、棋譜の開閉、解析結果挿入時の位置安定化 |

今回のプロトタイプは既存の盤・棋譜・評価グラフ・エンジン局面生成を再利用した。実装量の概算ではDが最小、Bが最大である。ただし本番ではURL復元、履歴、エラー、解析キャンセルまで追加評価が必要になる。

## 判断と根拠

本番案はまだ採用しない。Computer Use比較だけでは人間の発見性、安心感、現在状態の理解を証明できないためである。

次の初見ユーザーテストにはAを第一候補として進め、現行版BASEと比較する。Aは対局と解析の目的を明示的に分けられ、利用者が現在の作業を説明しやすい。また、今後それぞれの機能が増えても情報構造を保ちやすい。この理解しやすさと将来拡張性を、Computer Useで測った短期的な操作数より上位の製品判断基準とする。

この判断はB・Dの定量結果を否定しない。Bは400px幅の解析到達スクロールを最も減らし、Dは全体の操作量が最少だった。両案は比較証跡と代替候補として保持する。

Aは400px幅の合計スクロールを現行版から減らせていない。したがって、これは既知の採用リスクであり、初見テストの観察項目かつ本番設計時の改善条件とする。Aで現在のモードや局面を正しく理解できない、または狭い画面の負担を許容できない場合は、BまたはDを再検討する。

## 次のアクション

1. Aと現行版BASEを比較する初見ユーザーテストを最低5セッション実施する。
2. モバイル相当を最低2セッション含め、現在のモード・局面・手数・解析対象を正しく説明できるか確認する。
3. 400px幅で、対局開始・着手・棋譜確認・解析の上下移動と主観的負担を観察する。
4. 人間テストの結果を`prototype-results.csv`とExcel台帳へ追加する。
5. Aが判断ゲートを満たさない場合はBまたはDを再検討し、満たす場合のみ本番設計・実装Issueへ分割する。

## A案のパイロット反映（Issue #14）

1名の探索的パイロットとComputer Use模擬セッションで、Aの対局モードに外部KIF読込があると役割分離が弱くなることを確認した。特に、対局カードと共通棋譜カードの2か所に読込入口があり、進行中の対局を外部棋譜で置き換える意味も事前に示されていなかった。

この観察を受け、Aのプロトタイプでは次のように変更する。

- 対局モードは、新規対局、現在棋譜の局面移動、保存、共有に限定する。
- 外部KIF読込は解析モードの「KIFを読み込んで解析」1か所へ集約する。
- 進行中の対局・棋譜がある場合は、置き換わることとキャンセル時に保持されることを確認画面で示す。
- 読込後は解析モードを維持し、読込手数と表示局面をステータス通知する。

これはAの本番採用決定ではない。役割分離の仮説をより純粋に比較するためのプロトタイプ改善として扱い、変更後も初見ユーザーテストの判断ゲートは維持する。
